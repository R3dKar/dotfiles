(defwidget time-module []
  (box
    :spacing 15
    :space-evenly false

    (box
      :spacing 5
      :space-evenly false

      (icon :icon "󰥔")
      "${formattime(EWW_TIME, '%T')}"
    )
    (box
      :spacing 5
      :space-evenly false

      (icon :icon "")
      "${formattime(EWW_TIME, '%d.%m.%Y %a')}"
    )
  )
)
